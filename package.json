{
  "name": "dealership-voice-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Inbound voice AI receptionist for car dealerships — Vapi custom tools webhook (demo build)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "tsx src/index.ts",
    "simulate": "tsx scripts/simulate.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
